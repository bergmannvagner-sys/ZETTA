# Docker

Arquivos Docker principais:

- `../docker-compose.yml`: PostgreSQL e Redis para desenvolvimento local.
- `../backend/Dockerfile`: imagem do backend usada no Render.

Este diretório fica reservado para scripts ou configurações Docker futuras sem criar múltiplos backends.
